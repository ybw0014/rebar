package io.github.pylonmc.rebar.item.interfaces

/**
 * Allows an item to be placed as its vanilla block counterpart.
 *
 * For example, if you have a `Shiny Diamond Block` item and it implements
 * this interface, a player could place it down as a normal vanilla `Diamond Block`.
 */
interface VanillaBlockItem